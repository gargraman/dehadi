# ============================================================================
# Dehadi.co.in - Environment Configuration Template
# ============================================================================
# Copy this file to .env and update the values for your environment
# For local development: cp .env.example .env
# For production: Set these as environment variables in your deployment platform
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# ----------------------------------------------------------------------------
# PostgreSQL connection details
# For local Docker: Use the values below
# For production: Use your cloud database connection string

DATABASE_URL=postgresql://dehadi:dehadi_password@localhost:5432/dehadi_db
PGHOST=localhost
PGPORT=5432
PGUSER=dehadi
PGPASSWORD=dehadi_password
PGDATABASE=dehadi_db

# ----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# ----------------------------------------------------------------------------
# Environment: development | production
NODE_ENV=development

# Session secret for Express session management
# IMPORTANT: Generate a strong random string for production!
# You can use: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-random-secret-in-production

# Application Port (default: 5000)
PORT=5000

# ----------------------------------------------------------------------------
# PAYMENT GATEWAY (Razorpay)
# ----------------------------------------------------------------------------
# Get your keys from: https://dashboard.razorpay.com/app/keys
# Test Mode: Use test keys (prefix: rzp_test_)
# Live Mode: Use live keys (prefix: rzp_live_)

RAZORPAY_KEY_ID=rzp_test_your_key_id_here
RAZORPAY_KEY_SECRET=your_razorpay_secret_here

# Frontend needs the Key ID (not secret!)
VITE_RAZORPAY_KEY_ID=rzp_test_your_key_id_here

# ----------------------------------------------------------------------------
# OBJECT STORAGE (Replit Object Storage or S3-compatible)
# ----------------------------------------------------------------------------
# If using Replit Object Storage, these are auto-configured
# For AWS S3 or other providers, configure accordingly

DEFAULT_OBJECT_STORAGE_BUCKET_ID=
PUBLIC_OBJECT_SEARCH_PATHS=
PRIVATE_OBJECT_DIR=

# AWS S3 Configuration (if using AWS instead of Replit)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=ap-south-1
# AWS_S3_BUCKET=dehadi-assets

# ----------------------------------------------------------------------------
# AI FEATURES (OpenAI Integration)
# ----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# Optional: Only needed if using AI-powered features

OPENAI_API_KEY=

# ----------------------------------------------------------------------------
# AUTHENTICATION (OIDC/OAuth)
# ----------------------------------------------------------------------------
# Replit Auth configuration (auto-configured on Replit)
# For external OAuth providers, add their credentials here

ISSUER_URL=
CLIENT_ID=
CLIENT_SECRET=

# ----------------------------------------------------------------------------
# MONITORING & LOGGING
# ----------------------------------------------------------------------------
# Optional: Configure external logging and monitoring services

# Sentry for error tracking
# SENTRY_DSN=

# LogDNA/LogRocket for logging
# LOGDNA_KEY=

# ----------------------------------------------------------------------------
# PGADMIN (Local Development Only)
# ----------------------------------------------------------------------------
# Web interface for PostgreSQL database management
# Access at http://localhost:5050 when running docker-compose with --profile tools

PGADMIN_EMAIL=admin@dehadi.local
PGADMIN_PASSWORD=admin

# ----------------------------------------------------------------------------
# DEPLOYMENT NOTES
# ----------------------------------------------------------------------------
# 
# LOCAL DEVELOPMENT:
# 1. Copy this file: cp .env.example .env
# 2. Update DATABASE_URL to point to your local PostgreSQL
# 3. Add your Razorpay test keys
# 4. Run: npm run dev
#
# DOCKER LOCAL:
# 1. Update .env with your values
# 2. Run: docker-compose up -d
# 3. Access app at: http://localhost:5000
# 4. Access pgAdmin at: http://localhost:5050 (with --profile tools)
#
# AWS/CLOUD DEPLOYMENT:
# 1. Set environment variables in your deployment platform:
#    - ECS: Task Definition environment variables
#    - Elastic Beanstalk: Configuration > Software > Environment properties
#    - EC2: Export in .bashrc or use /etc/environment
# 2. Use AWS Secrets Manager or Parameter Store for sensitive values
# 3. Never commit .env files to version control!
#
# SECURITY CHECKLIST:
# ✓ Change SESSION_SECRET to a random 32+ character string
# ✓ Use environment-specific Razorpay keys (test vs live)
# ✓ Set strong PGPASSWORD for production database
# ✓ Enable SSL for DATABASE_URL in production
# ✓ Rotate secrets regularly
# ✓ Use IAM roles instead of access keys when possible (AWS)
# ============================================================================
